function money(input){
    let startMoney = Number(input) * 7.61;
    let discount = startMoney * 0.18;
    let finalPrice = startMoney - discount;
    console.log(`The final price is: ${finalPrice} lv.`)
    console.log(`The discount is: ${discount} lv.`)
}
